#8004表示注册到zookeeper服务器的支付服务提供者的端口号
server:
  port: 8004
  # 服务别名注册zookeeper导住的中心的名称
spring:
  application:
    name: cloud-provider-payment
  cloud:
    zookeeper:
      connect-string: 192.168.144.225:2181

